# 심화실습-1 보고서

<!-- 
👨🏻‍💻 작성요령
아래 양식에 맞춰 내용을 채워주세요.

⚠️ 주의 ⚠️
단, 해시테그(`#`)로 시작하는 줄은 수정하지 마세요.
제출 내용에서 해시테그로 시작하는 줄이 없어진 경우, 항목 누락으로 간주되어 감점이 있을 수 있습니다.

👀 참고자료
[1] GitHub, GitHub에서 쓰기 위한 빠른 시작, https://docs.github.com/ko/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/quickstart-for-writing-on-github
[2] GitHub, Markdown 기본 문법, https://docs.github.com/ko/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax
-->

## 1. 제출 정보

| 구분 | 내용 |
| --- | --- |
| 학년학기 | 2026학년도 1학기 |
| 과목명(DASF004_52) | 프로그래밍기초와실습 (DASF004-00) |
| 학번 | 2026031355 |
| 이름 | 노윤아 |
| 과제명 | 심화실습-1 |

## 2. 현재 상태

### 2.1 배운적 있는 언어 및 현재 수준
- Python - 해당없음
- Java - 해당없음
- C - 해당없음

### 2.2 실습 환경

|ChatGPT | GitHub Codespace |

## 3. 코드 리뷰 결과

### 3.1 AI가 생성한 코드를 얼마나 이해할 수 있었는지?

- 이해도: 0 ~ 10점 (0점: 전혀 이해하지 못함, 10점: 완전히 이해함)
- 어떤식으로 동작하는 코드로 이해했는지 자유롭게 서술

### 3.2 최초에 입력한 프롬프트와 그 결과물에 대한 평가

#### 최초 프롬프트

```
사용한 프롬프트를 복사 & 붙여넣기
```

#### 최종 코드 및 실행 결과

> 본 문서 가장 아래 `부록` 섹션에서 `최종 결과물 코드`와 `실행 결과`에 각각 코드 및 실행 결과를 복사 & 붙여넣기로 첨부합니다.

### 3.3 최종 결과물을 얻기까지의 경험

- 서술형으로 자유롭게 작성

## 4. 결론

- 서술형으로 자유롭게 작성
- 코드 분석 과정에서 어려웠던 점
- AI 시대 프로그래밍 학습에 대한 개인적인 견해나 느낀 점

## 참고문헌

- 사용한 AI 도구, 참고한 자료 등

---
---

## 부록

### 실행 결과

- 프로그램의 주요 기능 실행 화면을 터미널에서 텍스트를 복사&붙여넣기로 작성
- 프로그램 실행에 실패했다면, 실패와 관련된 내용을 담아도 좋음

```bash

```

### 최종 결과물 코드

- AI가 생성한 전체 코드 첨부

```c

```#include <stdio.h>
#include <string.h>

#define MAX_SCHEDULES 100
#define STR_LEN 100

// 일정 정보를 담는 구조체
typedef struct {
    char title[STR_LEN];
    char date[STR_LEN];  // 예: 2024-05-20
    char time[STR_LEN];  // 예: 14:30
    char desc[STR_LEN];
} Schedule;

Schedule list[MAX_SCHEDULES];
int count = 0;

// 함수 선언
void addSchedule();
void showAll();
void updateSchedule();
void deleteSchedule();

int main() {
    int choice;

    while (1) {
        printf("\n--- [일정 관리 프로그램] ---\n");
        printf("1. 일정 추가\n2. 일정 조회\n3. 일정 수정\n4. 일정 삭제\n5. 종료\n");
        printf("선택: ");
        scanf("%d", &choice);
        getchar(); // 버퍼 비우기

        switch (choice) {
            case 1: addSchedule(); break;
            case 2: showAll(); break;
            case 3: updateSchedule(); break;
            case 4: deleteSchedule(); break;
            case 5: printf("프로그램을 종료합니다.\n"); return 0;
            default: printf("잘못된 입력입니다.\n");
        }
    }
}

// 1. 일정 추가
void addSchedule() {
    if (count >= MAX_SCHEDULES) {
        printf("저장 공간이 가득 찼습니다!\n");
        return;
    }
    printf("\n[새 일정 추가]\n");
    printf("제목: "); fgets(list[count].title, STR_LEN, stdin);
    printf("날짜(YYYY-MM-DD): "); fgets(list[count].date, STR_LEN, stdin);
    printf("시간(HH:MM): "); fgets(list[count].time, STR_LEN, stdin);
    printf("설명: "); fgets(list[count].desc, STR_LEN, stdin);

    // 개행 문자 제거
    list[count].title[strcspn(list[count].title, "\n")] = 0;
    
    count++;
    printf("일정이 성공적으로 추가되었습니다.\n");
}

// 2. 일정 조회
void showAll() {
    if (count == 0) {
        printf("\n=> 저장된 일정이 없습니다.\n");
        return;
    }
    printf("\n--- [전체 일정 목록] ---\n");
    for (int i = 0; i < count; i++) {
        printf("[%d] 제목: %s | 날짜: %s | 시간: %s", i + 1, list[i].title, list[i].date, list[i].time);
    }
}

// 3. 일정 수정
void updateSchedule() {
    showAll();
    if (count == 0) return;

    int num;
    printf("\n수정할 일정 번호를 선택하세요: ");
    scanf("%d", &num);
    getchar();

    if (num < 1 || num > count) {
        printf("유효하지 않은 번호입니다.\n");
        return;
    }

    int idx = num - 1;
    printf("새 제목: "); fgets(list[idx].title, STR_LEN, stdin);
    printf("새 날짜: "); fgets(list[idx].date, STR_LEN, stdin);
    printf("새 시간: "); fgets(list[idx].time, STR_LEN, stdin);
    printf("새 설명: "); fgets(list[idx].desc, STR_LEN, stdin);
    
    list[idx].title[strcspn(list[idx].title, "\n")] = 0;
    printf("일정이 수정되었습니다.\n");
}

// 4. 일정 삭제
void deleteSchedule() {
    showAll();
    if (count == 0) return;

    int num;
    printf("\n삭제할 일정 번호를 선택하세요: ");
    scanf("%d", &num);
    getchar();

    if (num < 1 || num > count) {
        printf("유효하지 않은 번호입니다.\n");
        return;
    }

    for (int i = num - 1; i < count - 1; i++) {
        list[i] = list[i + 1]; // 뒤의 일정을 앞으로 당김
    }
    count--;
    printf("일정이 삭제되었습니다.\n");
}
